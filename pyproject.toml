[project]
name = "python-server"
version = "0.1.0"
description = "A small, dependency-free HTTP server for testing video ad playback"
readme = "README.md"
requires-python = ">=3.14"
dependencies = []
